#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Only run validation if YAML files were changed
if git diff --cached --name-only | grep -q "\.yml$\|\.yaml$"; then
  echo "📋 YAML files changed - running validation..."
  npm test
else
  echo "✅ No YAML files changed - skipping validation"
fi
