{
  "name": "224",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "test": "npm run validate-all",
    "validate-yaml": "node scripts/validate-yaml.js",
    "validate-exam": "node scripts/validate-exam-yaml.js",
    "validate-images": "node scripts/check-images.js",
    "validate-all": "npm run validate-yaml && npm run validate-exam && npm run validate-images",
    "validate-quiet": "QUIET=true npm run validate-all",
    "validate": "npm run validate-all",
    "build:leukocytes": "node scripts/generate-flashcards.js --source unit2-practical --tags leukocytes,formed-elements --output unit2-leukocytes",
    "build:heart-anatomy": "node scripts/generate-flashcards.js --source unit2-practical --tags heart,anatomy --output unit2-heart-anatomy",
    "build:heart-circulation": "node scripts/generate-flashcards.js --source unit2-practical --tags coronary-circulation,heart-lp-5 --output unit2-heart-circulation",
    "build:ekg": "node scripts/generate-flashcards.js --source unit2-practical --tags ekg --output unit2-ekg",
    "build:blood-disorders": "node scripts/generate-flashcards.js --source unit2-practical --tags anemia,blood --output unit2-blood-disorders",
    "build:all-unit2": "npm run build:leukocytes && npm run build:heart-anatomy && npm run build:heart-circulation && npm run build:ekg && npm run build:blood-disorders",
    "dev": "python3 -m http.server 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
